name: manual-run-2-example
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy environment'
        required: true
        default: 'staging'
      version:
        description: 'Version tag'
        required: false
